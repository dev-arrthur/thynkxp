'use client';
import { useEffect, useState } from 'react';
export default function CookieConsent(){const [open,setOpen]=useState(false);const [prefs,setPrefs]=useState(false);useEffect(()=>{setOpen(!document.cookie.includes('thynkxp_consent='));},[]);function save(analytics:boolean){document.cookie=`thynkxp_consent=${analytics?'analytics':'necessary'}; Max-Age=31536000; Path=/; SameSite=Lax`;setOpen(false);setPrefs(false);if(analytics) window.dispatchEvent(new CustomEvent('thynkxp:consent',{detail:{analytics:true}}));}if(!open)return <button className="cookie-fab" onClick={()=>setOpen(true)} aria-label="Preferências de cookies">🍪</button>;return <div className="cookie-panel"><div><b>🍪 Sua privacidade importa.</b><p>Usamos cookies necessários e, com sua autorização, analytics para entender o uso do site e melhorar sua experiência.</p><a href="/privacidade">Saiba mais</a></div>{prefs?<div className="cookie-prefs"><label><input type="checkbox" checked readOnly/> Necessários</label><label><input id="analytics" type="checkbox" defaultChecked/> Analytics</label><div><button onClick={()=>save((document.getElementById('analytics') as HTMLInputElement)?.checked)}>Salvar preferências</button></div></div>:<div className="cookie-actions"><button onClick={()=>save(false)}>Recusar opcionais</button><button onClick={()=>setPrefs(true)}>Preferências</button><button onClick={()=>save(true)}>Aceitar cookies</button></div>}</div>}