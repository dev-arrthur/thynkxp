import { NextResponse } from 'next/server';
export default function AdminLogin(){return <main className="login"><form onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(r.ok)location.href='/admin';else alert('Credenciais inválidas.');}}><span className="brand">thynk<span>XP</span></span><h1>Acesso administrativo</h1><p>Entre para acessar o CRM de leads.</p><input name="email" type="email" placeholder="E-mail" required/><input name="password" type="password" placeholder="Senha" required/><button>Entrar →</button></form></main>}